# Window Title Renamer

Rename a window title (including the taskbar button label) and optionally lock it to prevent changes. **Windows only**.

Standalone executable, No dependencies required.

## Usage

Download the tool from the **Releases** page and double-click the .exe file to run it. Then, simply follow the on-screen instructions.

# 窗口标题重命名工具

重命名窗口标题（包括任务栏按钮标签），并可选择将其锁定以防止更改。**仅限Windows**。

独立可执行文件，无需依赖其他库或程序。

## 用法

从**发布页面**下载该工具，然后双击.exe文件运行它。接下来，只需按照屏幕上的指示进行操作即可。
